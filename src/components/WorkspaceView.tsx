import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const WorkspaceView: React.FunctionComponent = () => <Container>Workspace!</Container>;

export default WorkspaceView;
