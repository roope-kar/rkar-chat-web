import React from 'react';
import styled from 'styled-components';

const Container = styled.div``;

const SigninForm: React.FunctionComponent = () => <Container>Signin</Container>;

export default SigninForm;
